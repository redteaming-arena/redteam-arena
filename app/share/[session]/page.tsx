import Header from "@/components/header";




export default async function SharedSession({params, searchParams} : { params : any, searchParams : any}) {
    
    const {session} = params;

    // check if session parameter 

    return <>Shared session</>    
}